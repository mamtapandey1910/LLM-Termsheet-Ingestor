POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=test
POSTGRES_USER=test
POSTGRES_PASSWORD=test

# Optional direct connection URL (takes precedence over fields above)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/termsheet

# Optional LLM provider key for extractor integration
OPENAI_API_KEY=you-test-api-key
